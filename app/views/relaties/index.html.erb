<h1>Listing relaties</h1>

<table>
  <tr>
    <th>Naam</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @relaties.each do |relaty| %>
  <tr>
    <td><%= relaty.naam %></td>
    <td><%= link_to 'Show', relaty %></td>
    <td><%= link_to 'Edit', edit_relaty_path(relaty) %></td>
    <td><%= link_to 'Destroy', relaty, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Relatie', new_relaty_path %>
